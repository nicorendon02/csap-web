/**
 * CSAP Events Data
 * ─────────────────────────────────────────────────────────────────
 * Add events here. They will automatically appear on:
 *   • The homepage (index.html) — shows the first 3 upcoming events
 *   • The events page (events.html) — shows all events
 *
 * If this array is empty, the homepage hides the events section
 * and the events page shows a "no upcoming events" message.
 *
 * Event fields:
 *   title       (string)  — Event name
 *   date        (string)  — Display date, e.g. "June 15, 2026" or "Date TBD"
 *   category    (string)  — "social" | "cultural" | "academic" | "food" | "fundraiser"
 *   description (string)  — Short description (1–2 sentences)
 *   location    (string)  — Venue / "Online" / "TBD"
 *   rsvpText    (string)  — Label for the action link, e.g. "RSVP on BoilerLink →"
 *   rsvpUrl     (string)  — URL for the RSVP link (leave "" to show plain text)
 *   icon        (string)  — Font Awesome icon class, e.g. "fa-champagne-glasses"
 * ─────────────────────────────────────────────────────────────────
 */
const CSAP_EVENTS = [

  // ── Paste new events below this line ──────────────────────────
  //
  // {
  //   title:       'Welcome Back Social',
  //   date:        'August 25, 2026',
  //   category:    'social',
  //   description: 'Join us for our semesterly kickoff — meet new members and reconnect with the CSAP family.',
  //   location:    'Purdue Campus',
  //   rsvpText:    'RSVP on BoilerLink →',
  //   rsvpUrl:     'https://boilerlink.purdue.edu/organization/csap',
  //   icon:        'fa-champagne-glasses',
  // },

];
